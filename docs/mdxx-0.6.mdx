---
title: amdx@0.6 - PWA Support, Lighthouse 100, RSS対応, コンポーネントの Tailwind 化
created: 1589106473785
tags: [amdx]
---

例によってこのブログ自身が実装例です。

![](https://gyazo.com/766cb68e83165ff078ae997f9f1f852e.png)

Accessbility のスコアがちょっと低いのは、 amp-social-share の生成する要素に `aria-label` がついてないから。どうしろと

## Done

### amdxg

- Lighthouse 100
- PWA Support
- RSS 対応 `/rss.xml`

### amdxg-cli

- `amdxg postbuild:rss` - rss 対応
- `amdxg postbuild:sitemap`: WIP - sitemap.xml 生成する(Search Console に認識されない…)

### amdxg-components

- Tailwindcss + postcss + purgecss
- Better CSS markup

## 次の TODO

- タグのサポート: frontmatter で `tags: [a,b,c]` とした場合、タグによるインデックスページを生成 `/tags/hoge`
